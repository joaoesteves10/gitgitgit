print("Insira um número:");
numA = input();

print("Insira outro numero");
numB= input();

operador = "Insira um operador dentro dos 4 possiveis {}, {}, {}, {}")
print(operador("+","-","x","/"));
operador= input();


if(variavel_operador == "+"):
    print(numA + numB);
else if(variavel_operador == "*"):
    print(numA * numB);
else if(variavel_operador == "-"):
    print(numA - numB);
else if(variavel_operador == "/"):
    print(numA / numB);
else:
print("ERRO");

